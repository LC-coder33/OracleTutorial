hm릴레이션 내의 point 컬럼의 값이 <50인 튜플의 개수를 구해라

hm릴레이션 내의 point 컬럼의 값이 <50인 튜플의 개수를 point컬럼을 기준 내림차순으로 출력해라

select 화면에 출력
from 대상 테이블
where 튜플 선정 조건
order by 정렬

select count(*) from hm where point < 50;

select * from hm where point < 50 order by point desc;

bcad


create table hm(
no number(3) primary key,
name varchar2(10) not null,
point number(5) default 0,
addr varchar2(20),
indate timestamp default sysdate);

insert into hm values (1,'홍길동',45,'서울시','24/09/02 12:10:41');
insert into hm values (2,'일지매',50,'수원','24/08/02 12:10:41');
insert into hm values (3,'이지매',43,'서울','24/07/02 12:10:41');

select * from hm order by no desc;
select * from hm order by point desc;
select * from hm order by point asc;
select no, name from hm;
select count(*) from hm;

select name, point from hm;
select name, point from hm where point < 50;
select count(*) from hm where point < 50;
select point from hm where name='홍길동';
select * from hm where name='일지매';

select no, name 이름
from hm
where point  < 50;

select name, point+10 가산포인트 from hm;

select name, point 현재포인트, point+10 가산포인트 from hm; 

update hm set point=100, name='김티쳐';

select * from hm;

delete from hm;

update hm set point=100, name='김티쳐' where name='홍길동';

delete from hm where name='김티쳐';

select concat('이름','님') 이름 from dual;
select * from hm where point >20 order by point desc;
select sysdate from dual;

select concat(name,'님'), point from hm;

select name, point, indate from hm order by indate desc;

select name, concat(concat(no, name), point) 정보 from hm where point >=50;

select name, point 현재포인트, 60-point 부족한포인트 from hm;

------------------------------------------------------------------------------------------------------------------------------------


1. 테이블 관리 : create table, drop table, alter table
                   : 테이블(릴레이션), 속성(속성타입, 제약조건)
	       : 타입 - int, number*, varchar2, timestamp...
	       : 제약 조건 - 유일성(기본키, unique), not null, check, default
	       : 키 - 기본키 - 유일성, not null, 인덱스 형성(검색속도 향상
						  - where 절에서 사용)
		- 외래키 - 컬럼 투 컬럼, 자식 릴레이션 컬럼이 부모
		           - 릴레이션의 컬럼을 참조한다.
                                   - 외래키 제약 조건 : cascade, set null
                   : ERD - 개체와 개체의 관계분석 1:1, 1:N, N:M 분석 후 테이블 형성

2. 테이블에 저장된 튜플의 조작
	삽입 - insert
	갱신 - update : 특정 튜플을 선택하여 갱신하려면 where절 사용
	삭제 - delete : 특정 튜플을 선택하여 삭제하려면 where절 사용
	검색 - select : 각 절의 의미 암기, 절의 실행 순서 이해
	        select절 : 컬럼 선택 - 별칭, 함수 등을 같이 사용할 수 있다.
	        from 절 : 대상 테이블
                    where 절 : 대상 테이블로부터 튜플을 선정할 조건
                    group by 절 : 그룹화
                    order by 절 : 출력할 때 정렬
3. 함수 : 함수를 호출하고 함수의 기능을 실행하고 결과값을 리턴.
           결과값을 리턴받아서 처리한다... 예를 들어 count(*)함수를 호출하면 튜플의 수를 카운팅하여 카운팅 결과값을 리턴한다.